use_default_settings: true

server:
  bind_address: '0.0.0.0'
  port: 8080
  limiter: false
  image_proxy: true
  secret_key: 'brok-public-search'

search:
  safe_search: 0
  formats:
    - html
    - json

ui:
  static_use_hash: true
